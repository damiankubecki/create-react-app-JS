import React from 'react';
import AppTemplate from 'templates/AppTemplate';
import { Wrapper, Title, Description } from './AppElements';

const App = () => (
  <AppTemplate>
    <Wrapper>
      <Title>Create React App JS</Title>
      <Description>The site is ready to work with JavaScript!</Description>
    </Wrapper>
  </AppTemplate>
);

export default App;
